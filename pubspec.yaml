name: automatic_animated_list
description: AnimatedList which computes the item deltas each time the underliying list gets updated, and animates the list tiles automatically.
version: 1.0.0
homepage: https://github.com/gkpln3

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
